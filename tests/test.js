const { describe, it } = require('mocha');
const assert = require('assert');

const Pin = require('./../src/visual/pin.js');


describe('Pin', () => {
  describe('test', () => {
    it('should work', () => {
      assert.equal('hi', 'hi');
    });
  });
});
